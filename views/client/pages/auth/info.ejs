<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> - Hà Nội Vibes</title>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --ff-primary: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            --ff-heading: 'Playfair Display', Georgia, 'Times New Roman', serif;
            --clr-brand: #1261A6;
            --clr-dark: #0f0f0f;
            --clr-card: rgba(255,255,255,0.9);
            --clr-card-dark: rgba(20,20,20,0.85);
        }

        * { box-sizing: border-box; }

        body {
            min-height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            /* Place image first, then subtle gradients, then base color */
            background:
                        url('/client/img/banner-home.jpg') center/cover no-repeat fixed,
                        radial-gradient(1200px 600px at 20% 10%, rgba(252,3,107,0.10), transparent 42%),
                        radial-gradient(1000px 800px at 90% 90%, rgba(108,43,217,0.10), transparent 42%),
                        #0b0b0c;
            background-blend-mode: normal, overlay, overlay, normal;
            color: #111;
            font-family: var(--ff-primary);
        }

        .auth-root {
            width: 100%;
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 24px 16px;
        }

        .login-wrap {
            width: 100%;
            max-width: 920px;
            display: grid;
            grid-template-columns: 1.1fr 1fr;
            gap: 0;
            border-radius: 18px;
            overflow: hidden;
            background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02));
            border: 1px solid rgba(255,255,255,0.08);
            box-shadow: 0 30px 60px rgba(0,0,0,0.35);
            backdrop-filter: blur(10px) saturate(150%);
        }

        .brand-side {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 48px 28px;
            background: linear-gradient(180deg, rgba(0,0,0,0.45), rgba(0,0,0,0.2));
            color: #fff;
        }

        .brand-logo {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
        }
        .brand-logo img { width: 44px; height: 44px; }
        .brand-title { font-family: var(--ff-heading); font-size: 1.75rem; letter-spacing: .12em; text-transform: uppercase; }
        .brand-slogan { color: #e5e5e5; margin: 6px 0 16px; font-size: 0.875rem; }

        .brand-hero {
            width: 100%;
            max-width: 460px;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.35);
            border: 1px solid rgba(255,255,255,0.08);
        }

        .form-side {
            background: var(--clr-card);
            backdrop-filter: blur(6px);
            padding: 42px 34px;
        }

        .form-title { font-weight: 700; font-size: 1.375rem; margin-bottom: 6px; }
        .form-sub { color: #666; margin-bottom: 18px; }

        .form-label { font-weight: 600; color: #333; }
        .input-group-text { background: #fff; border: 2px solid #e9ecef; border-right: none; }
        .form-control { border: 2px solid #e9ecef; padding: 12px 14px; }
        .input-group .form-control { border-left: none; }
        .form-control:focus { border-color: var(--clr-brand); box-shadow: 0 0 0 0.2rem rgba(252,3,107,0.15); }

        .btn-brand {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: linear-gradient(135deg, #1261A6, #ff4c9b);
            border: none;
            color: #fff;
            padding: 12px 16px;
            border-radius: 12px;
            font-weight: 700;
            letter-spacing: .2px;
            transition: transform .15s ease, box-shadow .2s ease;
        }
        .btn-brand:hover { transform: translateY(-1px); box-shadow: 0 10px 22px rgba(252,3,107,0.25); }
        .btn-brand:active { transform: translateY(0); }

        .btn-secondary {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: #6c757d;
            border: none;
            color: #fff;
            padding: 12px 16px;
            border-radius: 12px;
            font-weight: 700;
            letter-spacing: .2px;
            transition: transform .15s ease, box-shadow .2s ease;
            text-decoration: none;
        }
        .btn-secondary:hover { transform: translateY(-1px); box-shadow: 0 10px 22px rgba(108,117,125,0.25); color: #fff; }

        .meta { display:flex; align-items:center; justify-content:space-between; margin-bottom: 18px; }
        .meta a { color: var(--clr-brand); text-decoration: none; font-weight: 600; }

        .secure-note { color: #777; font-size: 0.8125rem; display:flex; align-items:center; gap:8px; }
        .secure-note i { color: var(--clr-brand); }

        .user-info {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 24px;
            margin-bottom: 24px;
        }

        .info-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #e9ecef;
        }

        .info-item:last-child {
            border-bottom: none;
        }

        .info-item label {
            font-weight: 600;
            color: #333;
            margin: 0;
        }

        .info-item span {
            color: #666;
        }

        .status {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 500;
        }

        .status.active {
            background: #d4edda;
            color: #155724;
        }

        .status.inactive {
            background: #f8d7da;
            color: #721c24;
        }

        @media (max-width: 900px) {
            .login-wrap { grid-template-columns: 1fr; }
            .brand-side { display: none; }
        }
    </style>
</head>
<body>
    <main class="auth-root">
        <div class="login-wrap">
            <aside class="brand-side">
                <div class="brand-logo">
                    <img src="/client/img/logo.jpg" alt="Hanoivibes">
                    <strong class="brand-title">Hanoivibes</strong>
                </div>
                <p class="brand-slogan">Thông tin tài khoản</p>
                <div class="brand-hero">
                    <img src="/client/img/hotay.jpg" alt="Hanoi" style="width:100%;height:auto;display:block;">
                </div>
            </aside>

            <section class="form-side">
                <h1 class="form-title">Thông tin tài khoản</h1>
                <p class="form-sub">Quản lý thông tin cá nhân của bạn</p>

                <% if (typeof flash !== 'undefined') { %>
                    <% if (flash.error && flash.error.length > 0) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="fas fa-exclamation-circle me-2"></i>
                            <%= flash.error[0] %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Đóng"></button>
                        </div>
                    <% } %>
                    <% if (flash.success && flash.success.length > 0) { %>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="fas fa-check-circle me-2"></i>
                            <%= flash.success[0] %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Đóng"></button>
                        </div>
                    <% } %>
                <% } %>

                <form class="user-info" action="/auth/info" method="post" enctype="multipart/form-data">
                    <div class="text-center mb-3">
                        <img id="avatarPreview" src="<%= user.avatar || '/client/img/avatar.png' %>" alt="Avatar" style="width:96px;height:96px;border-radius:50%;object-fit:cover;border:2px solid #e9ecef;">
                    </div>
                    <div class="info-item">
                        <label>Ảnh đại diện:</label>
                        <div>
                            <input id="avatarInput" type="file" name="avatar" accept="image/*" class="form-control form-control-sm">
                            <div class="form-text">Ảnh vuông, dung lượng &lt; 5MB</div>
                        </div>
                    </div>
                    <div class="info-item">
                        <label>Họ và tên:</label>
                        <input type="text" class="form-control" name="fullName" value="<%= user.fullName %>" required>
                    </div>
                    
                    <div class="info-item">
                        <label>Email:</label>
                        <input type="text" class="form-control" value="<%= user.email %>" readonly>
                    </div>
                    
                    <div class="info-item">
                        <label>Số điện thoại:</label>
                        <input type="text" class="form-control" value="<%= user.phone || 'Chưa cập nhật' %>" readonly>
                    </div>
                    
                    <div class="info-item">
                        <label>Trạng thái:</label>
                        <span class="status <%= user.status %>"><%= user.status === 'active' ? 'Hoạt động' : 'Bị khóa' %></span>
                    </div>

                    <div class="d-grid gap-2 mt-3">
                        <button type="submit" class="btn btn-brand">
                            <i class="fa fa-save"></i>
                            Lưu thay đổi
                        </button>
                    </div>
                </form>

                <div class="d-grid gap-2 d-md-flex">
                    <a href="/auth/logout" class="btn btn-secondary">
                        <i class="fa fa-sign-out-alt"></i>
                        Đăng xuất
                    </a>
                    <a href="/" class="btn btn-brand">
                        <i class="fa fa-home"></i>
                        Về trang chủ
                    </a>
                </div>

                <div class="mt-3 secure-note">
                    <i class="fa fa-shield-alt"></i>
                    <span>Thông tin tài khoản được bảo mật an toàn.</span>
                </div>
            </section>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            document.getElementById('avatarInput')?.addEventListener('change', function (e) {
                const file = e.target.files && e.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (ev) => {
                    const img = document.getElementById('avatarPreview');
                    if (img) img.src = ev.target.result;
                };
                reader.readAsDataURL(file);
            });
        </script>
</body>
</html>
